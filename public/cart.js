

/* async function printFromLocal() {
    let body = document.getElementById("body")
    let userCart = localStorage.getItem("cart", cart)
    console.log(userCart)

    body.innerText = userCart

    /* for (let i = 0; i < userCart.length; i++) {
        const element = userCart[i];
        console.log(element)


        let productcardContainer = document.createElement("div")
        productcardContainer.innerText = element

        productcardContainer.appendChild(body)

    } */


/* printFromLocal() */
